import React from 'react'

const AudioListItem = (props: {id: any, title: String, image:String, duration:String, artist:String}) => {
  return (
    <div>
        Each Audio Track
    </div>
  )
}

export default AudioListItem